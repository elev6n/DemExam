<UserControl x:Class="DemExam.Desktop.Views.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DemExam.Desktop.ViewModels"
             MaxWidth="250"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance {x:Type vm:AuthorizationViewModel} }">

    <Grid Margin="10" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="Авторизация" FontSize="24" FontWeight="Bold" Margin="5" />

        <Label Grid.Row="1" Grid.Column="0" Content="Логин:" Margin="5" />
        <TextBox Text="{Binding Login}" Grid.Row="1" Grid.Column="1" Margin="5" />

        <Label Grid.Row="2" Grid.Column="0" Content="Пароль:" Margin="5" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Margin="5" />

        <Button Grid.Row="3" Grid.ColumnSpan="2" Grid.Column="0" Content="Войти" Margin="5"
                Padding="10,5" Command="{Binding LoginCommand }"
                CommandParameter="{Binding ElementName=PasswordBox}" />

        <TextBlock Text="{Binding ErrorMessage}" Grid.Row="4" Grid.ColumnSpan="2" Grid.Column="0" Foreground="Red"
                   Margin="5" />
    </Grid>
</UserControl>