<Window x:Class="DemExam.Desktop.Views.CaptchaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="CaptchaWindow" Height="450" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Соберите пазл для продолжения" FontSize="16" FontWeight="Bold" Grid.Row="1" Grid.Column="1"
                   Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="10" />

        <Border BorderBrush="Gray" BorderThickness="1" Background="LightGray" Height="162" Width="162" Grid.Row="2"
                Grid.Column="1" Grid.ColumnSpan="2" Margin="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80" />
                    <RowDefinition Height="80" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" BorderBrush="Gray"
                        BorderThickness="1" />
                <Border Grid.Row="0" Grid.Column="1" BorderBrush="Gray"
                        BorderThickness="1" />
                <Border Grid.Row="1" Grid.Column="0" BorderBrush="Gray"
                        BorderThickness="1" />
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="Gray"
                        BorderThickness="1" />

                <Canvas Name="PuzzleCanvas" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" Grid.Column="0" />
            </Grid>
        </Border>

        <Button x:Name="ButtonCheck" Content="Проверить" Grid.Column="1" Grid.Row="3" Click="ButtonCheck_OnClick" />
        <Button x:Name="ButtonRefresh" Content="Обновить" Grid.Column="2" Grid.Row="3" Click="ButtonRefresh_OnClick" />

        <TextBlock x:Name="ResultText" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="4" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10" />
    </Grid>
</Window>