<Window x:Class="DemExam.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DemExam.Desktop.ViewModels"
        xmlns:views="clr-namespace:DemExam.Desktop.Views"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="768" MinWidth="1366">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AdminViewModel}">
            <views:AdminView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CreateEditUserViewModel}">
            <views:CreateEditUserView></views:CreateEditUserView>
        </DataTemplate>
    </Window.Resources>

    <StackPanel>
        <TextBlock Text="MainWindow" FontSize="24" FontWeight="Bold" />
        
        <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
    </StackPanel>
</Window>